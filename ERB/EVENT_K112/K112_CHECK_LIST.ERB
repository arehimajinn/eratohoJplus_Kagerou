;-------------------------------------------------
;チェックリスト用関数@K112_CHECK_LIST
;式中において呼び出された場合、引数の数字のコマンドが埋まっているかを確認する。
;-引数
;ARG = コマンドの数値（SELECTCOMを想定）
;-返り値
;-1 = 主導権その他関係無いです
;0 = 埋まっていない
;1 = 埋まっている
;2 = 主導権も埋まっている
;-------------------------------------------------
@K112_CHECK_LIST(COMMAND)
#FUNCTION
#DIM COMMAND

;-------------------------------------------------
;以下CASE （コマンドの数値）に埋めた所だけRETUNRF (返り値)
;色々つくりかたはある。DIMしてコマンドリストを羅列して数値を入れておくとか。
;-------------------------------------------------
SELECTCASE COMMAND
	;弾幕ごっこ
	CASE 300
		RETURNF -1
	;会話
	CASE 301
ENDSELECT
;ここまで来たら0
RETURNF 0
